# Skills

Put local skills in this folder.

A skill is a folder containing a `SKILL.md` file (AgentSkills/OpenClaw-style). Example:

```
skills/
  my-skill/
    SKILL.md
```

Configure skill directories + enabled skills in `agent_blob.json`:
- `skills.dirs`
- `skills.enabled`
- `skills.max_chars`

